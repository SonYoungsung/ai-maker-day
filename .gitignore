# dependencies / build
site/node_modules
site/dist

# env (anon 키라도 .env는 커밋하지 않음 — .env.example만 공유)
.env
.env.*
!.env.example

# os
.DS_Store

# OMC 운영 상태 (커밋 대상 아님)
.omc/

# TS 빌드 캐시
*.tsbuildinfo
